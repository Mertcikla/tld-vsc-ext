## Color Customization

By default, tlDiagram imports your VS Code theme's colors for the canvas, elements, and accent.

To use a custom palette instead, open **Settings** and search for `tldiagram color`.

[Configure colors](command:tldiagram.configureColors)
