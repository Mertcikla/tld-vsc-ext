## Install the tld CLI

The `tld` CLI powers all local diagram workflows inside VS Code.

```sh
# macOS / Linux
brew install Mertcikla/tap/tld

# or download from GitHub Releases
```

After installing, run the verify command to confirm everything is set up:

[Verify CLI](command:tldiagram.verifyCli)
